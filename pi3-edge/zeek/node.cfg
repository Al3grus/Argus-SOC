# Zeek Node Configuration — Pi 3B+ (argus-edge-01)
# Location: /etc/zeek/node.cfg

[zeek]
type=standalone
host=localhost
interface=eth1    # USB adapter — SPAN mirror destination, no IP, promiscuous mode
